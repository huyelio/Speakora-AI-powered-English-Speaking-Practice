import { NextResponse } from "next/server";
import { OpenAIProvider } from "../../../../modules/ai-gateway/openai";
import { readSessionToken } from "../../../../modules/practice/auth";
import { authorizeSession,questionBelongsToSession } from "../../../../modules/practice/repository";
export const runtime="nodejs";
export async function POST(request:Request){try{const body=await request.json().catch(()=>null);if(!body?.sessionId||!body?.sessionQuestionId)return NextResponse.json({error:"Session and question are required."},{status:400});if(!await authorizeSession(body.sessionId,readSessionToken(request)))return NextResponse.json({error:"Session not found."},{status:404});const question=await questionBelongsToSession(body.sessionId,body.sessionQuestionId);if(!question)return NextResponse.json({error:"Question not found."},{status:404});const prompt=(question.prompt_snapshot as any).prompt_text;const audio=await new OpenAIProvider().synthesize(prompt);return new NextResponse(audio,{headers:{"Content-Type":"audio/mpeg","Cache-Control":"private, max-age=3600"}});}catch(error){console.error("TTS failed",error);return NextResponse.json({error:"Unable to synthesize question."},{status:500});}}
