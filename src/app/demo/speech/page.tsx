import { PracticeSession } from "../../../components/practice/practice-session";

export default function SpeechDemoPage() {
  return <PracticeSession initialSession={null} principalKind="guest" />;
}
